## 窗口坐标系

glViewport和glReadPixels都是窗口坐标系，但原点都在左下角。

- `glViewport(x, y, width, height)` 定义了 渲染的视口区域。它会影响渲染操作的坐标系，但不会直接影响像素读取。
- 如果你裁剪了画布并调整了视口，`glReadPixels` 的起始位置仍然是相对于整个画布的左下角 `(0, 0)`，而不是视口的左下角。


